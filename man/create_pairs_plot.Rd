% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_viz_pairplot.R
\name{create_pairs_plot}
\alias{create_pairs_plot}
\title{Create Pairs Plot and Save Plot}
\usage{
create_pairs_plot(data, output_file, width = 16, height = 12)
}
\arguments{
\item{data}{A data frame containing the variables to plot.}

\item{output_file}{Path to save the output figure.}

\item{width}{Width of the plot in inches (default: 16).}

\item{height}{Height of the plot in inches (default: 12).}
}
\value{
None (saves the plot to a file).
}
\description{
Generates a pairs plot for all variables in the dataset using ggpairs and saves it to a file.
}
\examples{
data <- data.frame(a = rnorm(10), b = rnorm(10))
temp_file <- tempfile(fileext = ".png")
on.exit(unlink(temp_file))  # this cleans up the file after the example runs
create_pairs_plot(data, temp_file)
}
